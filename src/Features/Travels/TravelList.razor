@using Funpoly.Core
@inject IGameManager gameManager

@if (@isInitialized)
{
    <Column>
        @foreach (var team in teams)
        {
            <TravelListItem Team="@team"></TravelListItem>
        }
    </Column>
}